// Substitui react-native-keyboard-controller/src/specs/KeyboardBackgroundViewNativeComponent.ts
// (versão 1.18.5, conferido diretamente no pacote baixado do npm).
// Ver lib/codegen-stubs/NativeSafeAreaView.js para o motivo completo do
// uso de requireNativeComponent em vez de codegenNativeComponent.
import { requireNativeComponent } from "react-native";

export default requireNativeComponent("KeyboardBackgroundView");
