// Substitui react-native-gesture-handler/src/specs/RNGestureHandlerButtonNativeComponent.ts
// (versão 2.28.0, conferido diretamente no pacote baixado do npm).
// Ver lib/codegen-stubs/NativeSafeAreaView.js para o motivo completo do
// uso de requireNativeComponent em vez de codegenNativeComponent.
import { requireNativeComponent } from "react-native";

export default requireNativeComponent("RNGestureHandlerButton");
